{
  "name": "goose",
  "private": true,
  "description": "Root shim: the web build lives in web/. These delegate so npm run works from the repo root too.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "test": "npm --prefix web run test",
    "test:watch": "npm --prefix web run test:watch"
  },
  "dependencies": {
    "fast-check": "^4.10.1",
    "vitest": "^5.0.1"
  }
}
